<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <%= link_to "BookStreet", root_path, class:"navbar-brand", id:"logo"  %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to t("articles"), articles_path, class:"nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to t("bloggers"), users_path, class:"nav-link" %>
        </li>
        <% if !logged_in? %>
          <li class="nav-item">
            <%= link_to t("login"), login_path, class:"nav-link" %>
          </li>
          <li>
            <%= link_to t("signup"), login_path, class:"nav-link" %>
          </li>
        <% else %>
          <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            [ <%= current_user.username %> ]
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <li><%= link_to t("my_profile"), user_path(current_user), class:"dropdown-item" %></li>
            <li><%= link_to t("edit_profile"), edit_user_path(current_user), class:"dropdown-item" %></li>
            <li><%= link_to t("logout"), logout_path, class:"dropdown-item", method: :delete %></li>
            <li><hr class="dropdown-divider"></li>
            <li><%= link_to t("create_article"), new_article_path, class:"dropdown-item" %></li>
          </ul>
        </li>
        <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>
